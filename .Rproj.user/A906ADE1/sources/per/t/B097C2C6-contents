SAS BASE EXAM BLUEPRINT
=======================

1. Access and create data structures (20–25%)
---------------------------------------------

Skills needed:

- Importing & reading data
- DATA step: INFILE, INPUT (list, column, formatted input).
- PROC IMPORT / PROC EXPORT (Excel, CSV).
- LIBNAME for permanent libraries & external sources (e.g., Excel).
- Creating datasets
- Using DATA step to build new tables.
- Applying KEEP=, DROP=, RENAME=, LABEL.
- SASHELP & temporary vs. permanent datasets
- Understanding WORK vs. permanent libraries.

2. Manage data (35–40%)
-----------------------

This is the largest section of the exam.

- Sorting & organizing: PROC SORT with BY & OUT=.
- Filtering & subsetting: IF, WHERE (difference & exam traps), subsetting IF within DATA steps.
- Combining datasets: SET (concatenating), MERGE (one-to-one & match-merge with BY), interleaving (sorted data).
- Modifying variables: new variables, conditional assignment (IF-THEN-ELSE), functions for cleaning (MISSING, IFN(), COALESCE()).
- Working with dates & times: TODAY(), MDY(), INTNX(), INTCK().
- Using formats: PROC FORMAT (creating & applying formats).

3. Error handling (15–20%)
--------------------------

- Interpreting SAS logs: distinguishing errors, warnings, notes.
- Common errors: uninitialized variables, invalid merges, missing semicolons.
- Debugging DATA steps: using PUT statements, checking compilation vs. execution errors.

4. Generate reports and output (15–20%)
---------------------------------------

- Basic reporting: PROC PRINT (listing data), PROC REPORT (tabular reports), PROC TABULATE (multi-dimensional tables).
- Summarizing data: PROC MEANS (N, MEAN, STD, MIN, MAX), PROC FREQ (frequency tables, cross-tabs, CHISQ), PROC UNIVARIATE (distribution & histograms).
- Output Delivery System (ODS): ODS HTML, ODS PDF, ODS RTF, ODS SELECT / ODS EXCLUDE.

5. Must‑know practical traps
----------------------------

- Forgetting BY in MERGE → wrong data alignment.
- Confusing INPUT vs. PUT (informat vs. format).
- IF vs. WHERE differences (compile vs. execution phase).
- PROC SORT overwriting datasets unless OUT= used.
- Column position errors in fixed-width raw data.
- Understanding FIRST. and LAST. variables for grouped processing.

6. Memorize vs. recognize
-------------------------

- Memorize:

  -- DATA step core syntax (read, manipulate, merge).
  -- PROC PRINT, SORT, FREQ, MEANS, IMPORT, EXPORT.
  -- Key ODS commands.

- Recognize only:

  -- Advanced PROC REPORT/PROC TABULATE options.
  -- Some advanced PROC FORMAT uses.

7. Practice resources
---------------------

- Datasets: SASHELP.CLASS, CARS, HEART, SHOES.
- Mock exam data (for merges, reporting).
- Hands‑on debugging tasks (correcting broken code).
